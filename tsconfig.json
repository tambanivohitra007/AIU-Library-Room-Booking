{
  // Solution-style config: claims no files itself so the IDE and tsc always
  // use the real configs in client/ and server/ (each with its own deps).
  "files": [],
  "references": [{ "path": "./client" }, { "path": "./server" }]
}
